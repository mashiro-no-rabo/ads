study README.md

if there is a plan.md, follow the implementation plan in that file, do ONE THING ONLY.

Otherwise, look through all todos/ pick the most important thing to do, analyze the todo and write a plan.md then exit. Each todo file should have a header to mark the priority, e.g. CORE > LATER. Cleanup todos if it's completed.

IMPORTANT: update plan.md with progress, remove the file if all work is done, then update feature document in feats/

ALWAYS use `jj claude-commit [msg]` to commit the work then use `jj up` to push the changes. DO NOT use any other jj nor git commands.
